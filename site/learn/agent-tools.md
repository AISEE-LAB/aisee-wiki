---
title: Tool：Agent 真正执行动作的接口
permalink: /learn/agent-tools/
createTime: 2026/05/25 00:00:00
---

# Tool：Agent 真正执行动作的接口

Tool 是 Agent 把计划变成动作的接口。读文件、改代码、跑命令、查资料、调用 API，都通过工具完成。

<ClientOnly>
  <LearningFlow variant="tools" />
</ClientOnly>

## Tool 解决什么问题

模型只会生成文本。Tool 让它能观察环境、执行动作、看到结果，再根据结果调整下一步。没有 Tool，Agent 只能“建议”；有 Tool，Agent 才能“做事”。

## 常见工具类型

| 类型 | 典型动作 | 风险 |
|---|---|---|
| 文件工具 | 读文件、改文件、应用 patch | 误改无关文件、覆盖用户修改 |
| Shell | 安装依赖、启动项目、跑测试 | 执行危险命令、污染环境 |
| 浏览器 | 打开页面、点击、截图、检查布局 | 登录态、隐私、误操作 |
| 检索 | 搜索文档、读取官方资料 | 资料过期、来源不可靠 |
| 图像 | 生成图片、编辑素材、识别截图 | 版权、风格不一致 |
| Git | 查看状态、提交、推送 | 包含无关改动、错误分支 |
| 外部 API | 调业务服务或第三方接口 | 密钥、费用、生产影响 |

## 调用闭环

一个成熟的 Tool 调用不只是“执行命令”。它应该包含：选择工具、准备参数、执行、观察结果、解释影响、决定下一步。

::: warning 监督重点
当 Agent 准备删除文件、重置 Git、全局安装、访问网络、上传内容或修改部署配置时，应该要求它先解释影响和回滚方式。
:::

## Tool 与 MCP / Hook 的关系

MCP 可以把外部系统能力提供成 Tool；Hook 可以在 Tool 调用前后加检查点。Tool 负责动作，MCP 扩展动作来源，Hook 控制动作风险。

下一篇：[Hook：在关键节点加上自动检查和约束](/learn/hooks/)
