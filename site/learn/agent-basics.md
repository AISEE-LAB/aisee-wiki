---
title: Agent 是怎么工作的
permalink: /learn/agent-basics/
createTime: 2026/05/25 00:00:00
---

# Agent 是怎么工作的

Agent 不是更会聊天的模型，而是模型、上下文、工具、运行环境、约束和验证共同组成的执行系统。

<ClientOnly>
  <DiagramFigure
    src="/diagrams/learn/agent-loop.svg"
    title="Agent 的最小工作循环"
    caption="目标进入上下文，模型决定下一步；如果需要工具，就执行动作并把结果写回下一轮。"
  />
</ClientOnly>

## 发展原因

Chat 适合解释、讨论和生成片段。但真实工程任务通常需要读取项目、修改多个文件、运行命令、观察错误、继续修复。Agent 把这些动作串成一个闭环。

## Agent 的最小组成

| 组成 | 作用 | 常见失败点 |
|---|---|---|
| 模型 | 推理、生成计划、选择动作 | 误解目标、过度自信 |
| 上下文 | 当前任务、项目文件、历史对话 | 上下文缺失或污染 |
| Memory | 长期偏好、项目约定、历史决策 | 把旧记忆当事实 |
| Tool | 读文件、改代码、跑命令、查资料 | 权限过大或结果误读 |
| 环境 | Git、依赖、终端、浏览器、测试 | 环境不可复现 |
| 约束 | 项目规则、安全边界、完成标准 | 没有明确边界 |
| 验证 | 构建、测试、预览、人工审查 | 只看输出不看结果 |

## 工作循环

用户目标进入当前会话；Agent 读取项目规则和相关文件；模型形成计划；调用工具执行；观察输出和错误；必要时修正计划；最后给出结果、diff 和验证说明。

::: warning Agent 容易失败的位置
目标模糊、权限过大、没有验证、上下文污染、错误级联，是最常见的五类问题。解决方式不是“换更强模型”这么简单，而是把任务边界和检查点设计好。
:::

## 后续结构

Memory 解决“带着上下文工作”；Skill 解决“复用稳定流程”；MCP 解决“连接外部能力”；Tool 解决“执行动作”；Hook 解决“关键节点自动检查”。

下一篇：[Memory：让 Agent 带着上下文持续工作](/learn/agent-memory/)
