## 1. 组件接口与注册

- [x] 1.1 新增流程讲解 Vue 组件，支持标题、说明、阶段、节点、连接、状态、强调和可选链接输入
- [x] 1.2 为组件定义清晰的 props 类型或本地类型结构，覆盖阶段、节点、连接和图例数据
- [x] 1.3 在 `site/.vuepress/client.ts` 注册流程讲解组件，确保 Markdown 页面可直接调用
- [x] 1.4 确认组件不要求后端、数据库、鉴权、运行时 API、在线保存或外部图形服务

## 2. 展示行为与响应式

- [x] 2.1 实现桌面端阶段、节点和连接关系展示，保证主要文本和链接不重叠
- [x] 2.2 实现移动端纵向或分组降级，优先保证阶段顺序、节点说明和关系文本可读
- [x] 2.3 实现节点状态和重点强调，且状态不得只依赖颜色表达
- [x] 2.4 确认流程标题、阶段、节点和说明文本由代码或 Markdown 数据承载，不作为含文字图片展示
- [x] 2.5 实现长流程 timeline 模式，避免阶段过多时横向挤压
- [x] 2.6 增加克制的进入、hover 和 focus 动效，并支持 reduced motion 降级

## 3. 示例内容与栏目入口

- [x] 3.1 在 workflows 栏目新增或更新流程组件示例页，提供可访问的中文示例入口
- [x] 3.2 示例使用 OpenSpec、Compound/Harness、aisee 或软件工程流程相关场景，不使用无关占位数据
- [x] 3.3 示例明确为组件示例、建设中内容或基础示例，不声称 P0/P1 正文已经完成
- [x] 3.4 检查示例页链接只指向存在的正式栏目或资源入口，不产生站内死链

## 4. 静态边界与内容安全

- [x] 4.1 确认公开站点运行时不直接引用 `docs/requirements/`、`docs/tech-context/`、`docs/split/` 或其他 `docs/` 协作产物路径
- [x] 4.2 确认组件和示例不暴露维护者本地绝对路径、私有资料路径或未整理协作草稿
- [x] 4.3 确认未新增不必要依赖；如必须新增依赖，先更新 design 并说明原因
- [x] 4.4 确认基础动态效果未引入 Vue Bits、motion-v 或其他额外运行时依赖

## 5. 验证

- [x] 5.1 运行 `openspec validate "build-reusable-flow-explainer" --strict`
- [x] 5.2 运行静态检查，确认组件已注册、示例页存在、示例页不引用 `docs/` 协作产物路径
- [x] 5.3 运行路径检查，确认示例页引用的站内链接目标存在
- [x] 5.4 运行 `pnpm docs:build`
- [x] 5.5 如实现包含复杂视觉状态，使用本地预览或浏览器截图检查桌面和移动端可读性
- [x] 5.6 检查组件未使用厚重阴影、超过 1px 的侧边强调边框或不可降级动效
